name: snake_ai
description: A new Flutter project.

publish_to: "none"

version: 0.0.1

environment:
  sdk: ">=2.19.0-49.0.dev <3.0.0"

dependencies:
  flame: ^1.2.0
  flame_svg: ^1.4.0
  yaru: ^0.3.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
